**ImGuiPrinter**
A simple utility to send ImGui pages/layouts to a physical printer (A4 format).

*(prototype version in development).*

**Requirements**
Dear ImGui (v1.92+)
OS-specific backend (Windows Win32 GDI or Linux CUPS)
